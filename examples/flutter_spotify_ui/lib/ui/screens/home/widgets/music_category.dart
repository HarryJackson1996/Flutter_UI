import 'package:flutter/material.dart';

class MusicCategory extends StatelessWidget {
  final List list;

  const MusicCategory(this.list);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
